//! The main app display functionality.

#[derive(Default)]
pub struct App {}

impl App {
    pub fn render(&self, _content: ()) -> anyhow::Result<()> {
        // TODO(Quadri): Render the provided content using the graphic context
        Ok(())
    }
}
